name: Test-Action

on:
  workflow_dispatch:

jobs: 
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2

      - name: Build Github Runner docker image
        run: |
          echo "great success!"
